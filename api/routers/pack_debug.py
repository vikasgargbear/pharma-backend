"""
Pack Debug Router - Empty stub to fix import error
This router was referenced but never created
"""

from fastapi import APIRouter

router = APIRouter(
    prefix="/debug/pack",
    tags=["debug"]
)

# Empty router to satisfy import
# Can be expanded later if needed